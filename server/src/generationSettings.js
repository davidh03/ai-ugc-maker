export function clone(value) { return value == null ? value : JSON.parse(JSON.stringify(value)); }
export function normalizeGenerationSettings(input = {}) { return { durationSec: Number(input.durationSec || 15), style: input.style || 'product', music: Boolean(input.music), voiceover: Boolean(input.voiceover), voiceoverProvider: input.voiceoverProvider || 'openai-tts', voiceoverVoice: input.voiceoverVoice || 'coral', voiceoverOffsetSec: Number(input.voiceoverOffsetSec) || 0, durationMode: input.durationMode === 'voiceover' ? 'voiceover' : 'fixed', composer: input.composer || 'template', provider: input.provider || '', model: input.model || '', reasoningEffort: input.reasoningEffort || '', assets: clone(Array.isArray(input.assets) ? input.assets : []) }; }
export function settingsFromJob(job = {}) { return normalizeGenerationSettings(job.effectiveSettings || job); }
