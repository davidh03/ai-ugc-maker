const MAP={'visual.design':['compose','lint','render','complete'],'visual.scene':['compose','lint','render','complete'],'brief.content':['compose','lint','render','complete'],'asset.replace':['asset-analysis','compose','lint','render','complete'],'caption.text':['compose','lint','render','complete'],'narration.text':['voiceover','music','complete'],'narration.voice':['voiceover','music','complete'],'music.track':['music','complete'],'narration.timing':['music','complete'],'timing.scene':['compose','lint','render','voiceover','music','complete']};
export function planRevision(parent,changeSet={}) { const impacted=new Map(); for(const factor of changeSet.factors||[]) for(const stage of (MAP[factor.id]||['compose','lint','render','complete'])) { if(!impacted.has(stage)) impacted.set(stage,[]); impacted.get(stage).push(factor.id); } const voice=Boolean(parent.voiceover&&(parent.voiceoverMeta||parent.artifactManifest?.voiceoverAudio)); const clean=Boolean(parent.artifactManifest?.cleanVideo); const ids=['brief','web-research','asset-analysis','prompt-optimizer','compose','voiceover','lint','render','music','complete']; const nodes=ids.map(id=>{const causedBy=impacted.get(id)||[];let action=causedBy.length?'rerun':'reuse';let reason=causedBy.length?`Affected by ${causedBy.join(', ')}`:'Unchanged from source version';if(id==='voiceover'&&!causedBy.length)action=voice?'reuse':'rerun';if(id==='render'&&!causedBy.length)action=clean?'reuse':'rerun';if(id==='music'&&!(changeSet.settingsDiff?.after?.music||changeSet.settingsDiff?.after?.voiceover))action='skip';if(id==='complete')action='rerun';if(action==='rerun'&&id==='render'&&!clean&&!causedBy.length)reason='Clean visual render was not saved on this historical version';return {id,action,causedBy,reason};});return {nodes,summary:{rerun:nodes.filter(n=>n.action==='rerun').map(n=>n.id),reuse:nodes.filter(n=>n.action==='reuse').map(n=>n.id),skip:nodes.filter(n=>n.action==='skip').map(n=>n.id)}};}
