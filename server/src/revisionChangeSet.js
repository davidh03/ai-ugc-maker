import { diffSettings } from './revisionDiff.js'; import { analyzeRevisionIntent } from './revisionIntent.js';
export function buildChangeSet(parent,instruction,proposedSettings={}) { const intent=analyzeRevisionIntent(instruction); const settingsDiff=diffSettings(parent,proposedSettings); const factors=[...intent.factors]; for(const field of settingsDiff.changedFields){const id=field==='voiceoverProvider'||field==='voiceoverVoice'?'narration.voice':field==='voiceoverOffsetSec'?'narration.timing':field==='music'?'music.track':field==='style'?'visual.design':field==='durationSec'?'timing.scene':field==='assets'?'asset.replace':field==='voiceover'?'narration.text':`settings.${field}`;const existing=factors.find(f=>f.id===id);if(existing)existing.source='manual';else factors.push({id,source:'manual',operation:'change',evidence:`Setting changed: ${field}`,confidence:1});}return {instruction:instruction.trim(),factors,preserve:intent.preserve,ambiguities:intent.ambiguities,settingsDiff,analyzer:intent.analyzer}; }
