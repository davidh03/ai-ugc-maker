{
  "name": "ai-ugc-maker-server",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "start:node22": "nvm use && node src/index.js",
    "test": "NODE_ENV=test node --test src/*.test.js",
    "render": "nvm use && npx hyperframes render"
  },
  "dependencies": {
    "express": "^5.1.0",
    "@hyperframes/producer": "^0.8.0",
    "@hyperframes/sdk": "^0.8.0"
  }
}
